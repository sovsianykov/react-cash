name: Demo action
on: workflow_dispatch
jobs:
     prints:
           runs-on: ubuntu-latest
           steps: 
                - name: Print to console
                  run: echo Hello from github Actions!!!
  
  
